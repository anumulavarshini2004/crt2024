#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int w,a,c;
    scanf("%d%d%d",&w,&a,&c);
    int v=a*75+c*30;
    if(v<=w)
        printf("Boat is stable");
    else
        printf("Boat will drown");
    
    return 0;
}
