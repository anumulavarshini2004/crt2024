#include <stdio.h>

int main()
{
    int m,n;
    scanf("%d%d",&m,&n);
    int a[m][n],i,j;
    for(i=0;i<n;i++)
    {
        for(j=0;j<m;j++)
        {
            scanf("%d",&a[j][i]);
        }
    }
    for(i=0;i<n;i++)
    {
        for(j=0;j<m;j++)
        printf("%d ",a[j][i]);
        printf("\n");
        
    }
}
