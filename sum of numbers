#include <stdio.h>

int main() {
    int num;
    long long sum = 0;

     while (1) {
        scanf("%d", &num);

        if (num == -1) {
            break;
        }

        sum += num;
    }

    
    printf("%lld\n", sum);

    return 0;
}
